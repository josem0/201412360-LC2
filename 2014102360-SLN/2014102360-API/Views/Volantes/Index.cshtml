@model IEnumerable<_2014102360_ENT.Volante>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Volantes</h2>

<p>
    @Html.ActionLink("Nuevo Volante", "Create")
</p>
<table id="tbl_Volantes" class="table table-bordered table-hover">
    <thead>
        <tr>
            <th>Numero Serie Volante</th>
            <th>Editar</th>
            <th>Detalles</th>
            <th>Eliminar</th>

        </tr>
    </thead>

    <tbody></tbody>
    @*<tr>
        <th>
            @Html.DisplayNameFor(model => model.NumSerie)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NumSerie)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.VolanteId }) |
            @Html.ActionLink("Details", "Details", new { id=item.VolanteId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.VolanteId })
        </td>
    </tr>
}*@

</table>
@section scripts
{
    <script>
        $(document)
            .ready(function () {
               var table = $("#tbl_Volantes")
                    .DataTable({
                ajax: {
                    url: "/api/Volantes",
                    dataSrc:""
                },
                columns: [
                    { data: "NumSerie" },

                    {
                        data: "VolanteId",
                        render: function (data, type, doctor) {
                            return "<a href='/Volantes/Edit/" + data + "'>Actualizar</a>";
                        }
                    },
                    {
                        data: "VolanteId",
                        render: function (data, type, doctor) {
                            return "<a href='/Volantes/details/" + data + "'>Detalles</a>";
                        }
                    },
                    {
                        data: "VolanteId",
                        render: function (data) {
                            return "<a class='btn btn-primary' href='/Volantes/Delete/" + data + "'>Borrar</a>";
                        }
                    },

                ]

            });
        });
    </script>
}
