@model IEnumerable<_2014102360_ENT.Ensambladora>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Ensambladoras</h2>

<p>
    @Html.ActionLink("Nueva Ensambladora", "Create")
</p>
<table id="tbl_Ensambladoras" class="table table-bordered table-hover">
    <thead>
        <tr>
            <th>Nombre de la Ensambladora</th>
            <th>Tipo de Carro</th>
            <th>Editar</th>
            <th>Detalles</th>
            <th>Eliminar</th>

        </tr>
    </thead>

    <tbody></tbody>
    @*<tr>
        <th>
            @Html.DisplayNameFor(model => model._Ensambladora)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TipoCarro)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item._Ensambladora)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TipoCarro)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.EnsambladoraId }) |
            @Html.ActionLink("Details", "Details", new { id=item.EnsambladoraId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.EnsambladoraId })
        </td>
    </tr>
}*@

</table>

@section scripts
{
    <script>
        $(document)
            .ready(function () {
               var table = $("#tbl_Ensambladoras")
                    .DataTable({
                ajax: {
                    url: "/api/Ensambladoras",
                    dataSrc:""
                },
                columns: [
                    { data: "_Ensambladora" },
                    { data: "TipoCarro" },

                    {
                        data: "EnsambladoraId",
                        render: function (data, type, doctor) {
                            return "<a href='/Ensambladoras/Edit/" + data + "'>Actualizar</a>";
                        }
                    },
                    {
                        data: "EnsambladoraId",
                        render: function (data, type, doctor) {
                            return "<a href='/Ensambladoras/details/" + data + "'>Detalles</a>";
                        }
                    },
                    {
                        data: "EnsambladoraId",
                        render: function (data) {
                            return "<a class='btn btn-primary' href='/Ensambladoras/Delete/" + data + "'>Borrar</a>";
                        }
                    },

                ]

            });
        });
    </script>
}


