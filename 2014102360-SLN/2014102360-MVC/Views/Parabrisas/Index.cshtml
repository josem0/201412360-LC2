@model IEnumerable<_2014102360_ENT.Parabrisas>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Parabrisas</h2>

<p>
    @Html.ActionLink("Nueva Parabrisa", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NumSerie)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NumSerie)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ParabrisasId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ParabrisasId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ParabrisasId })
        </td>
    </tr>
}

</table>
